-- ret, ok = call("resetMarket"); assert(ok, ret)

-- ret, ok = call("issue", "uuid1", "apart1", 100); assert(ok, ret)
-- ret, ok = call("issue", "uuid2", "apart2", 100); assert(ok, ret)
-- ret, ok = call("issue", "uuid3", "apart2", 100); assert(ok, ret)

-- ret, ok = call("buy","uuid1"); assert(ok, ret)
-- ret, ok = call("buy","uuid2"); assert(ok, ret)

-- ret, ok = call("confirm", "uuid1"); assert(ok, ret)
